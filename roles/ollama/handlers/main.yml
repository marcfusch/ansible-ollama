# roles/ollama/handlers/main.yml
- name: Recharger et redémarrer ollama
  ansible.builtin.systemd:
    name: ollama.service
    state: restarted
    daemon_reload: true
    scope: user
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ ollama_uid.stdout }}"
